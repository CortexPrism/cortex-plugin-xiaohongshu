{
  "tasks": {
    "test": "deno test --allow-all --no-check test/**/*.test.ts",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["require-await"]
    }
  }
}
